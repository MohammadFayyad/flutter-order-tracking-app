# Environment Variables Template
# Copy this file to .env and fill in your actual values

# ============================================
# MAPBOX CONFIGURATION
# ============================================
# Get your Mapbox access token from: https://account.mapbox.com/
# Required for maps and geocoding functionality
MAPBOX_ACCESS_TOKEN=your_mapbox_access_token_here

# ============================================
# INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Replace 'your_mapbox_access_token_here' with your actual Mapbox token
# 3. Never commit the .env file to version control
# 4. The .env file is already in .gitignore

# ============================================
# HOW TO GET MAPBOX ACCESS TOKEN
# ============================================
# 1. Go to https://www.mapbox.com/
# 2. Sign up for a free account
# 3. Go to https://account.mapbox.com/
# 4. Copy your default public token
# 5. Paste it above replacing 'your_mapbox_access_token_here'

# ============================================
# FIREBASE CONFIGURATION
# ============================================
# Firebase configuration is handled by firebase_options.dart
# Run 'flutterfire configure' to set up Firebase
#
# IMPORTANT: The google-services.json file contains sensitive data
# and should NOT be committed to version control.
# It's already in .gitignore, but if it's already in your repo,
# you should remove it using:
#   git rm --cached android/app/google-services.json
#   git commit -m "Remove google-services.json from version control"

